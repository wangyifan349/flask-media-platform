"""Alternative executable entry point for Flask Media Platform."""
from app import app, run_application

# Running this file starts the same fixed Flask application as app.py.
if __name__ == "__main__":
    run_application()
